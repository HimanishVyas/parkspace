# Pinned exactly, and audited with pip-audit. The set this replaced was ~9
# months stale and carried known CVEs in three security-critical packages:
# pyjwt (token handling), python-multipart (upload parsing) and starlette
# (the whole request path).
fastapi==0.141.1
starlette==1.6.0
uvicorn[standard]==0.53.0
sqlalchemy[asyncio]==2.0.54
asyncpg==0.31.0
alembic==1.20.0
pydantic==2.13.5
pydantic-settings==2.15.0
email-validator==2.3.0
pyjwt==2.14.0
bcrypt==5.0.0
python-multipart==0.0.32
httpx==0.28.1
redis==8.1.0
tzdata==2026.4
